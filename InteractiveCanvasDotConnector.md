OK, this is another rule file. It is really important. The key is that when you drag a pointer, it always measures the distance based on already existing nodes that have been sent from the backend. This is really important. That new nodes orient themselves on fixed nodes and that through this the distance is calculated through them. There is a concrete, accurate measurement of the area.
